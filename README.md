Sublime Text Ruby Logger
============

Sublime snippet to log (with dashes).

Type `log` and hit tab. You'll get:

```
p "--------------------------------------------------"
p "thing:", thing
p "--------------------------------------------------"
```
